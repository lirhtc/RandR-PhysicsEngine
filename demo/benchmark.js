function benchmark(loops) {
    loops = loops || 10;
    let count = []
    for (let i = 0; i < loops < i++) {
        let start = new Date().getTime();
        let i = 10000;
        while (i-- > 0) {

        }
        let end = new Date().getTime();
        count.push(end - start);
    }

}